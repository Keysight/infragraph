components:
  schemas:
    Inventory:
      description: |-
        A container for all infrastructure devices and links.
      type: object
      properties:
        devices:
          description: |-
            A globally unique name for the device.
          type: array
          items:
            $ref: ./device.yaml#/components/schemas/Device
          x-field-uid: 1
        links:
          description: |-
            A globally unique name for the device.
          type: array
          items:
            $ref: ./device.yaml#/components/schemas/Link
          x-field-uid: 2